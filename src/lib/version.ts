// Bump this on each deploy so we can confirm the live site is up to date.
// If the version shown in Settings doesn't match, the deploy didn't land.
export const APP_VERSION = "2026.07.09 · v175 (Faster & leaner: parallelized DB round-trips, memoized heavy client work, removed ~300 lines of dead code — no behavior change)";
