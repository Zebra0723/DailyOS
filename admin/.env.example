# Copy from the DailyOS Supabase project (Settings -> API). Same database.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key = full DB access (bypasses RLS). Server-only secret.
SUPABASE_SERVICE_ROLE_KEY=
# This admin site's own URL, for the magic-link redirect.
NEXT_PUBLIC_SITE_URL=https://dailyos-admin.vercel.app
